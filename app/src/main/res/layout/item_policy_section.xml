<?xml version="1.0" encoding="utf-8"?>
<!-- res/layout/item_policy_section.xml -->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical">

    <!-- Section Header Row -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:layout_marginBottom="12dp">

        <TextView
            android:id="@+id/tv_section_number"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:background="@drawable/bg_section_number"
            android:gravity="center"
            android:textColor="@color/pp_white"
            android:textSize="13sp"
            android:textStyle="bold"
            android:text="@string/_1" />

        <TextView
            android:id="@+id/tv_section_title"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="12dp"
            android:textColor="@color/pp_black"
            android:textSize="15sp"
            android:textStyle="bold"
            android:fontFamily="serif"
            android:text="@string/section_title" />

    </LinearLayout>

    <!-- Section Body -->
    <TextView
        android:id="@+id/tv_section_body"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginStart="44dp"
        android:textColor="@color/pp_text_body"
        android:textSize="13sp"
        android:lineSpacingMultiplier="1.7"
        android:text="@string/body_content_here" />

</LinearLayout>